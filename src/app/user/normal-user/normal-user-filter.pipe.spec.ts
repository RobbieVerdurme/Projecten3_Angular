import { NormalUserFilterPipe } from './normal-user-filter.pipe';

describe('NormalUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
